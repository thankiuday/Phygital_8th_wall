/**
 * 3D mark image — must use real transparency (alpha < 255 on “empty” pixels).
 * `phygital-mark.png` in this repo was fully opaque (dark bg baked in); the
 * transparent asset is `phygital-logo.png`. Replace this path if you add a new
 * cube-only PNG/SVG with alpha.
 */
export const BRAND_MARK_SRC = '/phygital-logo.png';
