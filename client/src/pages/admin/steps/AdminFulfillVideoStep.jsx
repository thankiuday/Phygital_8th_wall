import Step3Video from '../../campaigns/steps/Step3Video';

/**
 * Admin fulfill — video uploads only (card image + QR placement come from the user request).
 */
const AdminFulfillVideoStep = () => <Step3Video fulfillMode />;

export default AdminFulfillVideoStep;
